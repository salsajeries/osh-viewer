/*
import { DataSynchronizer } from 'osh-js/core/timesync/DataSynchronizer.js';

// Mutable global object
const globals: Record<string, any> = {};
const timeSynchronizer: any = {}

export default {
  install(app) {
    app.config.globalProperties.$globals = globals;
  }
};

*/
